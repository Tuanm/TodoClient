<Window x:Class="Todo.Views.NewTaskViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Todo.Views"
        xmlns:controls="clr-namespace:Todo.Controls"
        AllowsTransparency="True"
        WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen">
    <Window.Background>
        <SolidColorBrush Opacity="0" Color="Black"/>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Name="container" Grid.Row="0"
                    Orientation="Vertical" HorizontalAlignment="Center"/>
        <StackPanel Name="menu" Grid.Row="1"
                    Orientation="Horizontal" HorizontalAlignment="Center"/>
    </Grid>
</Window>
